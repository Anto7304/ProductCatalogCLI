Interface represent the contract on what the repository will implement

Struct represent the blue print of the database

i have used uuid than using int in id because of:
1. security purposes( hard to gaze) 
2. its unique in all databases 
3. no conflict while merging database because of its uniqueness
